
# P3.5b wheel cache for offline plugin install — populated by
# seed-plugins.sh, shipped via the offline bundle (not git).
volumes/plugin_daemon/wheels/

# Pre-seeded .difypkg files are regenerated by seed-plugins.sh (and
# re-zipped by Phase 3's uv.lock URL rewrite). Ride the offline
# bundle, not git.
volumes/plugin_daemon/plugin_packages/
